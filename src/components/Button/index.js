// import { Button as _Button } from 'react-bootstrap';
// import { bsContextStyle, BS_CONTEXTS } from '../../utils';
// export const Button = bsContextStyle(_Button, 'default', [...BS_CONTEXTS, 'link']);

export { default as Button } from './Button';
